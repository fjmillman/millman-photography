import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import Button, { ButtonType } from '.';

<Meta title="Components/Buttons/Button" component={Button} />

# Button

This is our standard button component

<Canvas>
  <Story name="Default">
    <Button onClick={action('Clicked default button')}>
      <p>Default Button</p>
    </Button>
  </Story>
</Canvas>

You can specify the following button types to modify the look

<Canvas>
  <Story name="Primary">
    <Button type={ButtonType.PRIMARY} onClick={action('Clicked primary button')}>
      <p>Primary Button</p>
    </Button>
  </Story>
  <Story name="Secondary">
    <Button type={ButtonType.SECONDARY} onClick={action('Clicked secondary button')}>
      <p>Secondary Button</p>
    </Button>
  </Story>
</Canvas>

There's even 3 special buttons to denote certain states: success, warning, and error.

<Canvas>
  <Story name="Success">
    <Button type={ButtonType.SUCCESS} onClick={action('Clicked success button')}>
      <p>Success Button</p>
    </Button>
  </Story>
  <Story name="Warning">
    <Button type={ButtonType.WARNING} onClick={action('Clicked warning button')}>
      <p>Warning Button</p>
    </Button>
  </Story>
  <Story name="Error">
    <Button type={ButtonType.ERROR} onClick={action('Clicked error button')}>
      <p>Error Button</p>
    </Button>
  </Story>
</Canvas>
